# Быстрый старт для загрузки на GitHub

## Что нужно сделать:

### 1. Установите Git (если еще не установлен)
Скачайте с https://git-scm.com/download/win и установите

### 2. Создайте репозиторий на GitHub
- Зайдите на https://github.com
- Нажмите "New repository"
- Назовите репозиторий (например: `auto-monitor-bot`)
- Выберите Private (рекомендуется)
- НЕ ставьте галочку "Initialize with README"
- Нажмите "Create repository"

### 3. Выполните команды в терминале (в папке C:\bot):

```bash
git init
git add .
git commit -m "Initial commit: Auto-monitor Telegram bot"
git branch -M main
git remote add origin https://github.com/ВАШ_USERNAME/НАЗВАНИЕ_РЕПОЗИТОРИЯ.git
git push -u origin main
```

**Замените:**
- `ВАШ_USERNAME` - ваш логин на GitHub
- `НАЗВАНИЕ_РЕПОЗИТОРИЯ` - название репозитория, которое вы создали

### 4. При первом push GitHub попросит авторизацию:
- Используйте Personal Access Token вместо пароля
- Создайте токен: GitHub → Settings → Developer settings → Personal access tokens → Generate new token
- Выберите права: `repo` (полный доступ к репозиториям)

## ✅ Готово!

Ваш код теперь на GitHub. Файл `.env` с токеном НЕ будет загружен (благодаря .gitignore).

## 📝 Подробная инструкция

Смотрите файл `GITHUB_SETUP.md` для детальной информации.
