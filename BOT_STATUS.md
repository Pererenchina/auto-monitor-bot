# Текущее состояние бота - Авто-Монитор

## 📋 Общее описание

Telegram-бот для автоматического мониторинга объявлений о продаже автомобилей на 4 белорусских площадках:
- **av.by** - Автобарахолка
- **kufar.by** - Кufar (раздел Авто)
- **ab.onliner.by** - Автобарахолка Onlíner
- **abw.by** - Автобарахолка ABW

Бот работает 24/7, автоматически проверяет новые объявления каждые **15 минут** и отправляет уведомления пользователям о найденных автомобилях, соответствующих их фильтрам.

---

## 🎯 Основные функции

### 1. **Создание фильтров поиска**

Пользователь может создать неограниченное количество фильтров с различными параметрами:

#### Доступные параметры фильтрации:
- **Марка автомобиля** - выбор из 30+ популярных марок (BMW, Mercedes-Benz, Audi, Toyota, Volkswagen и др.)
- **Модель** - выбор модели в зависимости от выбранной марки (модель нельзя выбрать без марки)
- **Год выпуска** - диапазон от/до (например, от 2020 до 2024)
- **Цена** - диапазон в USD (от/до, например, от $10,000 до $50,000)
- **Коробка передач** - Автомат / Механика / Вариатор
- **Тип двигателя** - Бензин / Дизель / Электро

#### Интерфейс:
- **Главное меню** - постоянно закреплено рядом с полем ввода (ReplyKeyboard)
  - ➕ Добавить фильтр
  - 📋 Мои фильтры
  - ℹ️ Помощь
- **Inline-кнопки** - для выбора параметров фильтра
- **FSM (Finite State Machine)** - пошаговый ввод параметров фильтра

### 2. **Управление фильтрами**

- **Просмотр всех фильтров** - команда "Мои фильтры" показывает все созданные фильтры с пагинацией (если фильтров много)
- **Редактирование фильтров** - можно изменить любой параметр существующего фильтра
- **Удаление фильтров** - можно удалить ненужные фильтры
- **Активация/деактивация** - фильтры можно временно отключить без удаления

### 3. **Автоматический мониторинг**

#### Как работает:
1. **Периодическая проверка** - каждые 15 минут бот проверяет все активные фильтры
2. **Параллельный поиск** - для каждого фильтра проверяются все 4 площадки одновременно
3. **Умная фильтрация**:
   - Парсеры применяют фильтры на уровне запросов к сайтам (где возможно)
   - Дополнительная локальная фильтрация для точности
   - Проверка валидности данных (наличие title, правильный URL)
4. **Защита от дублей** - каждое объявление сохраняется в БД с уникальным ключом (source + ad_id)
5. **Ограничение проверки** - проверяются только первые 30 самых новых объявлений для оптимизации

### 4. **Уведомления пользователям**

#### Формат уведомления:
```
🚗 Новое объявление!

[Название автомобиля: Марка Модель Год]

📅 Год: 2022
🛣️ Пробег: 45,000 км
⚙️ Объем: 2.0 л
📍 Город: Минск
🔧 Коробка: Автомат
⛽ Двигатель: Бензин

💰 $25,000 82,500 BYN

🔗 Открыть объявление
```

#### Особенности:
- **Автоматическая конвертация валют** - если есть только USD или только BYN, вторая валюта вычисляется (курс 1 USD = 3.3 BYN)
- **Фото автомобиля** - если доступно, отправляется с фото
- **Правильные ссылки** - все ссылки ведут непосредственно на объявление, а не на главную страницу сайта
- **Защита от спама** - задержка 1 секунда между уведомлениями
- **Обработка ошибок** - ошибки "chat not found" обрабатываются корректно (для тестовых пользователей)

---

## 🔧 Технические детали

### Парсеры (parsers/)

#### 1. **KufarParser** (`kufar_parser.py`)
- **Метод**: API запросы к `api.kufar.by`
- **Особенности**:
  - Извлечение данных из `ad_parameters` (марка, модель, год, цена)
  - Поддержка различных форматов цены (USD/BYN)
  - Задержка 1 секунда между запросами для избежания rate limiting
  - Обработка параметров в формате `{'pl': 'Марка', 'vl': 'BMW', 'p': 'brand'}`

#### 2. **AvByParser** (`av_by_parser.py`)
- **Метод**: Парсинг HTML с извлечением JSON из `__NEXT_DATA__` script tag
- **Особенности**:
  - Retry логика с экспоненциальным backoff (до 3 попыток)
  - Задержки между запросами для избежания 429 ошибок
  - Fallback механизм - если фильтрованный поиск не дает результатов, пробует без фильтров
  - Извлечение данных из `properties` массива
  - Brand mapping для корректного поиска по ID марок

#### 3. **OnlinerParser** (`onliner_parser.py`)
- **Метод**: HTML парсинг с использованием **Playwright** (headless браузер)
- **Особенности**:
  - Playwright для рендеринга JavaScript-контента
  - Fallback на cloudscraper, если Playwright недоступен
  - Поиск элементов по классам `vehicle-form__offers-unit`
  - Извлечение brand/model из URL структуры `/brand/model/id`
  - Фильтрация нерелевантных элементов (новости, каталог и т.д.)
  - Timeout 15 секунд для загрузки контента

#### 4. **AbwParser** (`abw_parser.py`)
- **Метод**: HTML парсинг с BeautifulSoup
- **Особенности**:
  - Поиск объявлений по классам `card`, `item`, `listing`
  - Извлечение цены из текста (паттерны "52 495 р. 18 000 $")
  - Извлечение brand/model из URL и заголовка
  - Локальная фильтрация (abw.by не поддерживает URL-фильтры)

### База данных (SQLite)

#### Таблицы:

1. **users_filters** - фильтры пользователей
   - `id`, `user_id`, `brand`, `model`, `year_from`, `year_to`
   - `price_from_usd`, `price_to_usd`, `transmission`, `engine_type`
   - `is_active`, `created_at`

2. **found_cars** - найденные объявления
   - `id`, `filter_id`, `source`, `ad_id`, `title`
   - `price_usd`, `price_byn`, `year`, `mileage`, `engine_volume`
   - `city`, `url`, `image_url`, `transmission`, `engine_type`
   - `notified`, `found_at`

#### Особенности:
- Уникальность по `source + ad_id` (защита от дублей)
- Связь ForeignKey между фильтрами и найденными автомобилями
- Автоматическое удаление связанных записей при удалении фильтра

### Мониторинг (monitor.py)

- **Scheduler**: APScheduler (AsyncIOScheduler)
- **Интервал**: 15 минут
- **Логирование**: Все действия логируются в `bot.log`
- **Обработка ошибок**: Каждый парсер работает независимо, ошибки одного не влияют на другие

---

## 📊 Поддерживаемые марки и модели

### Марки (30+):
BMW, Mercedes-Benz, Audi, Lexus, Volvo, Tesla, Volkswagen, Opel, Toyota, Nissan, Honda, Mazda, Mitsubishi, Subaru, Suzuki, Hyundai, Kia, Renault, Peugeot, Citroen, Skoda, Ford, Chevrolet, Fiat, BelGee, LADA, Geely, Chery, BYD, Haval, Great Wall, Dongfeng, FAW, Changan

### Модели:
Каждая марка имеет свой список моделей (от 5 до 20+ моделей). Модель можно выбрать только после выбора марки.

---

## 🛡️ Защита и оптимизация

### Защита от проблем:
1. **Rate Limiting**:
   - Задержки между запросами (1-3 секунды)
   - Retry логика с экспоненциальным backoff
   - Ограничение количества проверяемых объявлений (30 на фильтр)

2. **Валидация данных**:
   - Проверка наличия title (минимум 3 символа)
   - Проверка правильности URL (не generic, не содержит 'filter')
   - Дополнительная фильтрация после парсинга

3. **Обработка ошибок**:
   - Try-except блоки во всех критических местах
   - Логирование всех ошибок с traceback
   - Graceful degradation (если один парсер упал, остальные продолжают работать)

4. **Производительность**:
   - Асинхронное выполнение (asyncio)
   - Ограничение проверки до 30 объявлений на фильтр
   - Кэширование парсеров (не пересоздаются каждый раз)

---

## 📝 Логирование

Все действия бота логируются в файл `bot.log`:
- Запуск/остановка бота
- Создание/редактирование/удаление фильтров
- Проверка объявлений по каждому фильтру
- Количество найденных объявлений на каждом источнике
- Отправка уведомлений
- Ошибки парсеров и их детали

---

## ⚙️ Конфигурация

- **Интервал проверки**: 15 минут (настраивается в `main.py`)
- **Курс валют**: 1 USD = 3.3 BYN (для конвертации)
- **Лимит объявлений**: 30 на фильтр за проверку
- **Задержка между уведомлениями**: 10 секунд

---

## 🚀 Запуск

```bash
python main.py
```

Бот автоматически:
1. Инициализирует базу данных
2. Запускает сервис мониторинга
3. Выполняет первую проверку через 5 секунд после запуска
4. Начинает периодические проверки каждые 15 минут

---

## 📈 Текущий статус

✅ **Работает:**
- Создание и управление фильтрами
- Мониторинг всех 4 площадок
- Парсинг объявлений с извлечением всех полей
- Фильтрация по всем параметрам
- Отправка уведомлений с полной информацией
- Конвертация валют
- Защита от дублей

⚠️ **Известные особенности:**
- av.by и abw.by иногда возвращают объявления, не соответствующие фильтрам (проблема серверной фильтрации сайтов)
- Локальная фильтрация исправляет эту проблему
- Playwright требуется для корректной работы ab.onliner.by (установка: `playwright install`)

---

## 🔄 Последние улучшения

1. ✅ Исправлено извлечение цены из ad_parameters в kufar.by
2. ✅ Добавлена конвертация валют во всех парсерах и уведомлениях
3. ✅ Улучшено формирование title (brand + model + year)
4. ✅ Исправлена обработка ошибок "chat not found"
5. ✅ Улучшено логирование успешных отправок уведомлений
6. ✅ Исправлена ошибка с переменной `year` в kufar_parser.py
